# SigCLI 분석 정리 (한국어)

이 문서는 SigCLI 저장소를 살펴보며 나눈 질의응답을 정리한 것이다.
설치·사용법부터 아키텍처, 정체성(CLI/스킬/MCP), 수익화 검토까지 한 번에 훑을 수 있도록 구성했다.

## 저장소 주소

| 구분     | 주소                                                                   |
| -------- | ---------------------------------------------------------------------- |
| 본가     | https://github.com/sigcli/sigcli                                       |
| 이 포크  | https://github.com/bmshin94/sigcli                                     |
| 공식 문서 | https://sigcli.ai                                                      |
| npm (CLI) | https://www.npmjs.com/package/@sigcli/cli                             |
| npm (스킬) | https://www.npmjs.com/package/@sigcli/skills                         |
| 이슈     | https://github.com/sigcli/sigcli/issues                                |

기준 시점 본가 저장소 지표: ⭐ 290 stars · 🍴 25 forks · 생성 2026-04-18 · TypeScript · MIT 라이선스

---

## 1. SigCLI는 무엇인가

한 줄 요약: **사용자가 브라우저로 한 번 로그인해두면, AI 에이전트가 자격증명을 보지 않은 채 그 계정으로 일할 수 있게 해주는 CLI 도구.**

슬로건은 `README.md`의 "Sign in your way. AI works on your behalf."

### 해결하는 문제

AI 에이전트에게 사내 시스템(Jira, 위키, 그룹웨어) 작업을 시키려면 자격증명이 필요하다. 그런데

- AI는 SSO·2FA 로그인을 스스로 통과할 수 없다.
- 쿠키나 토큰을 직접 넘기면 대화 로그·셸 히스토리·환경변수에 평문으로 남는다.
- 사내 시스템은 API 토큰 발급 자체가 막혀 있는 경우가 많다.

SigCLI는 비밀을 자신이 보관하고, 에이전트에게는 "요청을 대신 보내주는 창구"만 제공하는 방식으로 이 문제를 푼다.

### 동작 흐름

```
사용자가 브라우저로 로그인  →  sig가 쿠키/토큰 추출 + 암호화 저장  →  에이전트가 sig로 요청
   (SSO, MFA 무엇이든)           (~/.sig/, AES-256-GCM)              (sig request / sig proxy)
```

1. `sig login <url>` — 실제 Chrome을 띄운다. 사용자가 평소처럼 로그인한다.
2. CDP(Chrome DevTools Protocol)로 쿠키와 localStorage를 추출한다 (`cli/src/strategies/browser/`).
3. AES-256-GCM으로 암호화해 `~/.sig/`에 저장한다. 키는 `~/.sig/encryption.key` (권한 0400).
4. 에이전트는 `sig request` 등으로 요청만 던진다. 비밀값은 보지 못한다.

### 자격증명 사용 방식 (보안 강도 순)

| 방식                   | 설명                                                                    | 강도   |
| ---------------------- | ----------------------------------------------------------------------- | ------ |
| `sig get`              | 헤더 값을 출력한다. 기본은 마스킹 처리됨                                | 디버깅용 |
| `sig run -- <cmd>`     | `SIG_<PROVIDER>_*` 환경변수로 주입 후 실행, 출력에서 값 자동 마스킹     | 중     |
| `sig request <url>`    | sig가 직접 HTTP 요청을 보내고 결과만 돌려준다                            | 높음   |
| `sig proxy start`      | 로컬 MITM 프록시가 통신 중 주입. 자격증명이 프록시 프로세스 밖으로 안 나감 | 최상   |

`cli/src/commands/run.ts`의 사용법 안내에도 "환경변수는 리눅스에서 `/proc`으로 읽히니 더 강한 격리가 필요하면 `sig proxy`를 쓰라"는 문구가 들어 있다.

### 부가 기능

- `sig idp add` — TOTP(6자리 2FA) 시크릿을 암호화 저장해두면 로그인 중 자동 입력·제출
- `sig watch` — 만료 전 자격증명 자동 갱신 (cron)
- `sig sync push/pull` — SSH로 헤드리스 서버 등 다른 머신과 동기화
- 감사 로그 — 모든 접근 이력 기록 (`cli/src/audit/audit-log.ts`)
- `validateUrl` / `validateRule` — 실제로 로그인됐는지 검증 (공개 사이트는 비로그인 방문자에게도 쿠키를 주기 때문에 필요)

---

## 2. 저장소 구조

| 경로               | 역할                                                                       |
| ------------------ | -------------------------------------------------------------------------- |
| `cli/`             | 본체. `@sigcli/cli` npm 패키지, `sig` 명령어. TS 소스 95개, 테스트 45개     |
| `skills/`          | AI 에이전트용 스킬 14종. 파이썬 스크립트 262개                              |
| `packages/skills`  | `npx @sigcli/skills` — 스킬을 코딩 에이전트에 설치해주는 인스톨러            |
| `sdk/typescript`   | `@sigcli/sdk` — 저장된 자격증명을 읽는 경량 TS SDK                          |
| `sdk/python`       | `sigcli-sdk` — 파이썬 SDK                                                   |
| `website/`         | sigcli.ai 공식 문서 사이트 (TanStack Start + React 19)                      |
| `talks/`           | 발표 자료 (Agentic Conf 2026 덱)                                            |
| `.claude/`         | 이 저장소 전용 에이전트 팀 설정 (architect/dev/tester/reviewer)              |

### cli/src 주요 디렉터리

- `commands/` — CLI 명령어 17개 (init, doctor, get, login, request, status, logout, providers, remote, sync, watch, rename, remove, completion, proxy, idp, run)
- `strategies/` — `browser`(CDP 기반), `oauth2`, `prompt`
- `storage/` — DirectoryStorage(파일 잠금 + 암호화), CachedStorage, MemoryStorage
- `crypto/` — AES-256-GCM 암복호화
- `proxy/` — MITM 프록시 데몬 (CA 관리, 프록시 서버, 데몬, 상태 파일)
- `audit/`, `apply/`, `config/`, `idps/`, `sync/`, `watch/`, `utils/`

### 문서와 코드의 불일치

`CLAUDE.md`에 적힌 구조는 실제 코드보다 뒤처져 있다.

- 문서상 `src/deps.ts`, `src/core/`, `src/cli/commands/`, `src/browser/adapters/` → 실제로는 `src/commands/`, `src/types/`, `src/cli-router.ts`
- 브라우저는 어댑터 패턴 대신 **CDP 직접 연결** 방식으로 전환됨 (`strategies/browser/cdp-ws.ts`)
- `idps/`, `audit/`, `apply/`, `config/` 디렉터리는 문서에 아직 없음
- `README.md`가 참조하는 `pitch/` 폴더는 저장소에 존재하지 않음

---

## 3. 설치 및 사용법

### 준비물

- Node.js 20.18.1 이상 (`cli/package.json`의 engines)
- Chrome 또는 Edge — 없으면 `sig doctor`가 실패한다 (`cli/src/commands/doctor.ts`)

### 설치

```bash
npm install -g @sigcli/cli    # sig 명령어 설치
sig init                      # ~/.sig/config.yaml 생성 (대화형)
sig doctor                    # 환경 점검
```

### 첫 로그인

```bash
sig login https://jira.example.com
```

Chrome 창이 뜨고, 평소처럼 로그인하면 설정까지 자동 생성된다(auto-provision).

### 주요 명령어

```bash
sig status                      # 로그인된 서비스 목록
sig request <url>               # 인증된 HTTP 요청 (권장)
sig run -- <cmd>                # 환경변수 주입 후 명령 실행
sig proxy start                 # MITM 프록시 시작 (가장 안전)
sig providers                   # 설정된 provider 목록
sig logout [provider]           # 자격증명 삭제
```

### 스킬 설치

```bash
npx @sigcli/skills              # 대화형 선택
npx @sigcli/skills --all        # 전부 설치
npx @sigcli/skills slack x      # 골라서 설치
npx @sigcli/skills --agent cursor
```

설치 경로는 에이전트별로 다르다 — `~/.claude/skills/`, `~/.cursor/skills/`, `~/.windsurf/skills/`, `~/.cline/skills/` (`packages/skills/bin/install.js`).

---

## 4. 플러그인인가, 스킬인가, MCP인가

**정답: CLI 도구다.** 스킬은 함께 제공되는 부가물이다.

| 분류     | 해당 여부 | 설명                                                    |
| -------- | --------- | ------------------------------------------------------- |
| CLI      | ✅ 본체    | `sig` 명령어. 터미널에서 실행하는 프로그램              |
| 스킬     | ✅ 부가    | `skills/` 안의 14종. SKILL.md + 파이썬 스크립트          |
| MCP 서버 | ❌        | 아님                                                     |
| 플러그인 | ❌        | 특정 툴에 꽂는 형태가 아님                               |

### MCP에서 CLI로 전환한 흔적

`.claude/agents/architect.md`에는 아직 "sigcli project — a TypeScript **MCP server** for browser-based authentication"이라는 설명이 남아 있다. 과거에는 MCP 서버였다는 뜻이다.

전환 이유는 `cli/CLAUDE.md`에 명시돼 있다.

> "The skill shells out to the CLI — **no MCP server needed**."

MCP는 도구 정의를 컨텍스트에 상주시켜야 하지만, CLI는 Bash로 호출하면 끝이라 더 가볍다는 판단이다.

---

## 5. API 토큰이 필요한가

**대부분 필요 없다.** 이것이 가장 큰 차별점이다.

| 전략       | 토큰 필요 | 용도                                    |
| ---------- | --------- | --------------------------------------- |
| `browser`  | ❌ 불필요  | 기본값. 브라우저로 직접 로그인해 쿠키 추출 |
| `oauth2`   | ⭕ 필요    | client_id / client_secret (Outlook, Teams) |
| `prompt`   | ⭕ 필요    | 토큰을 직접 입력                        |

`skills/README.md` 기준 14개 스킬 중 **12개가 Cookie 방식**이다. Slack, X, Reddit, LinkedIn, YouTube 등은 토큰 발급 없이 로그인만 하면 된다.

> ⚠️ 주의: 쿠키 추출 방식은 각 서비스 이용약관의 자동화·스크래핑 조항에 저촉될 소지가 있다. 본인 계정을 본인이 쓰는 것과, 이를 상품화하는 것은 법적으로 다른 문제다.

---

## 6. 왜 주목받는가

먼저 사실 확인: **⭐ 290 stars, 🍴 25 forks, 2026년 4월 생성.** "매우 유명한" 수준은 아니고, 신생 프로젝트가 틈새 분야에서 주목받는 단계다.

스타를 모은 요인:

1. **타이밍** — AI 에이전트 확산 + "인증은 어떻게 처리하나"라는 공통 고민을 정확히 겨냥
2. **뾰족한 문제 정의** — "Give agents access, not your credentials"
3. **즉시 체험 가능** — 스킬 14종이 이미 준비돼 있어 설치 후 바로 사용 가능
4. **데모 GIF** — README 상단의 움직이는 데모
5. **진지한 보안 설계** — AES-256-GCM, 감사 로그, MITM 프록시, 출력 마스킹

리스크 요인:

- 이슈 15개가 열려 있고, 사실상 1인 메인테이너 체제
- 문서와 코드의 불일치 — 빠르게 변화 중이라는 신호
- 쿠키 방식은 서비스가 UI를 바꾸면 깨진다 → 지속적 유지보수 필요

---

## 7. 로컬 에이전트 구축에 유용한가

### 크게 유용한 경우

"내 계정으로 웹 서비스를 조작하는 에이전트"를 만들 때다. 예를 들어

- 메일·슬랙을 요약해 아침 보고서를 만드는 비서 봇
- 사내 대시보드를 수집해 리포트를 만드는 봇
- SNS 모니터링 + 답변 초안 작성 봇

이런 작업은 인증이 난관의 대부분을 차지하는데, SigCLI가 그 부분을 통째로 처리한다. 직접 구현하면 브라우저 자동화 + 쿠키 관리 + 만료 갱신 + 암호화까지 상당한 작업량이 된다.

### 덜 유용한 경우

- 공식 API가 있는 서비스만 쓴다면 API 키 방식이 더 안정적이다.
- 에이전트가 로컬 파일과 코드만 다룬다면 필요 없다.

### 코드 자체의 참고 가치

- 출력 마스킹 — `cli/src/utils/redact.ts`
- 감사 로그 설계 — `cli/src/audit/`
- Result 패턴 — 예외를 던지지 않는 에러 처리
- 스킬 작성법 — `skills/slack/SKILL.md`의 "셋업 확인 → 검증 → 실행" 구조는 에이전트 지시문의 좋은 예시다

---

## 8. React나 PHP로 만들 수 있는가

### React — 불가능

React는 브라우저 안에서 동작하는 UI 라이브러리다. SigCLI의 작업은 대부분 브라우저 밖에서 일어난다.

| SigCLI의 작업            | React로 가능 여부              |
| ------------------------ | ------------------------------ |
| Chrome 실행 및 원격 제어 | ❌ 브라우저가 브라우저를 못 띄움 |
| `~/.sig/` 파일 저장      | ❌ 파일시스템 접근 불가          |
| 다른 프로그램 실행       | ❌ 프로세스 실행 불가            |
| MITM 프록시 서버 구동    | ❌ 서버 구동 불가                |
| 타 사이트 쿠키 읽기      | ❌ 브라우저 보안 정책이 차단     |

다만 **관리 UI는 React로 만들 수 있다.** 실제로 `website/`가 React 19로 작성돼 있다.

### PHP — 절반 정도 가능

| 기능                          | PHP로 가능 여부                       |
| ----------------------------- | ------------------------------------- |
| 자격증명 암호화 저장          | ✅ `openssl_encrypt`                   |
| HTTP 요청에 헤더 주입         | ✅ cURL                                |
| 만료 확인 / 자동 갱신         | ✅ cron                                |
| OAuth2 토큰 교환              | ✅                                     |
| 다른 프로그램 실행            | ⭕ 가능하나 깔끔하지 않음              |
| 브라우저 SSO 로그인 캡처      | ❌ 핵심 장벽                            |
| MITM 프록시 데몬              | ❌ 장기 실행 데몬에 부적합              |

### 언어별 적합도

| 언어       | 적합도                                                          |
| ---------- | --------------------------------------------------------------- |
| Node/TS    | 원본과 동일. 가장 수월함                                        |
| Python     | 충분히 가능. Playwright/Selenium + `mitmproxy`로 프록시는 오히려 쉬움 |
| Go         | 가능. 단일 바이너리 배포가 장점                                 |
| PHP        | 60% 정도                                                        |
| React      | UI 레이어 전용                                                  |

Node/TypeScript를 선택한 핵심 이유는 **Chrome 원격 제어(CDP)** 생태계다 (`cli/src/strategies/browser/cdp-ws.ts`).

> 권장: 새로 만들기보다 포크해서 스킬을 추가하는 편이 효율적이다. 스킬은 `SKILL.md` 한 장 + 파이썬 스크립트라 진입 장벽이 낮다.

---

## 9. 수익화 검토

상세 전략 문서: https://claude.ai/code/artifact/23105e38-3ae9-4912-90a1-b9e070ab47f5

### 전제

MIT 라이선스이므로 **코드 접근권으로는 과금할 수 없다.** 누구나 포크해서 팔 수 있기 때문이다.

| 팔 수 없는 것                  | 팔 수 있는 것                          |
| ------------------------------ | -------------------------------------- |
| `sig` CLI 자체 (npm 무료 배포) | 커버리지 — 아무도 만들지 않은 연동     |
| 기존 스킬 14종 (공개 저장소)   | 운영 — 24시간 대신 구동                |
| 성능 차별화 (병목은 브라우저)  | 통제 — 조직 단위 권한 관리·감사        |
|                                | 시간 — 도입 대행                        |
|                                | 신뢰 — 깨지면 고쳐준다는 약속           |

### 다섯 가지 모델

| 모델                    | 첫 매출까지 | 초기 투입           | 수익 상한 | 가장 큰 리스크                        |
| ----------------------- | ----------- | ------------------- | --------- | ------------------------------------- |
| A. 한국 서비스 스킬팩   | 1~2개월     | 주말 개발 몇 회     | 중        | 서비스 약관 · 셀렉터 깨짐             |
| B. 팀·기업용 오픈코어   | 9~18개월    | 제품팀 + 보안 인증  | 최상      | 1인 프로젝트로는 기업 보안심사 난관   |
| C. 매니지드 호스팅      | 6~12개월    | 인프라 + 보안 설계  | 중상      | 타인 자격증명 보관 — 유출 시 회복 불가 |
| D. 도입 구축 용역       | 2~6주       | 본인 시간           | 하        | 확장 불가                             |
| E. 자사 제품에 내장     | 해당 없음   | 0원                 | 간접      | 업스트림 중단 시 포크 유지보수        |

가격은 모두 가설이며 고객 인터뷰로 검증이 필요하다.

#### A. 한국 서비스 스킬팩 (권장 출발점)

스킬 14종은 전부 글로벌 서비스이고 **한국 서비스는 하나도 없다.** 카카오워크, 잔디, 두레이, 네이버웍스, 더존 ERP, 홈택스 등은 공개 API가 없거나 기업 심사를 요구해 브라우저 로그인이 사실상 유일한 접근 경로다.

- 가격 가설: 개인 9,900원/월, 팀 5석 49,000원/월
- 구독 근거: 서비스가 UI를 바꾸면 셀렉터가 깨진다. 이를 대신 고쳐주는 것이 상품 가치다

#### B. 팀·기업용 오픈코어 (수익 상한 최대)

현재는 1인용 도구다. 기업용 기능의 뼈대는 이미 절반쯤 있다 — 감사 로그(`src/audit/`), 자동 갱신(`src/watch/`), 원격 동기화(`src/sync/`). 여기에 권한 제어와 관리 콘솔을 얹으면 상품이 된다. 선례는 Tailscale, HashiCorp Vault, Infisical.

#### C. 매니지드 호스팅 (현시점 보류 권장)

브라우저가 필요하다는 물리적 제약이 과금 지점이지만, 타인의 자격증명을 서버에 보관하게 된다. 유출 한 번의 대가가 너무 크다.

#### D. 도입 구축 용역 (현금흐름 최속)

제품 없이 즉시 매출이 가능하고, 무엇보다 **유료로 수행하는 고객 인터뷰** 역할을 한다. B의 요구사항을 수집하는 경로가 된다.

#### E. 자사 제품에 내장 (간접)

MIT이므로 그대로 가져다 쓸 수 있다. 재사용 가치가 높은 부분은 `utils/redact.ts`(마스킹), `audit/`(이력), `sig proxy`(격리 구조)다.

### 90일 실행 순서

| 기간     | 단계        | 내용                                                    | 게이트                                   |
| -------- | ----------- | ------------------------------------------------------- | ---------------------------------------- |
| 1~14일   | 신호 확인   | 한국 서비스 스킬 1개만 만들어 무료 공개, 커뮤니티 반응 관측 | "다른 것도 만들어 달라" 요청 5건 이상    |
| 15~45일  | 유료화 시험 | 요청 많은 순으로 3~4개 추가, 번들 + 유지보수 구독 사전판매 | 실제 결제 10건 또는 파일럿 계약 1건      |
| 46~90일  | 방향 결정   | 구독이 붙으면 A 확장, 기업 문의가 오면 B 전환, 둘 다 약하면 D | 하나의 모델에 집중 (동시 진행 금지)      |

### 착수 전 정리할 세 가지

1. **약관** — 스킬 12개가 쿠키 추출 방식이다. 상품화하는 순간 각 서비스의 자동화·스크래핑 조항이 적용된다.
2. **개인정보** — B나 C로 가면 타인의 자격증명을 보관하게 된다. 처리방침·위탁계약·암호화 의무가 따르므로 법률 검토가 선행돼야 한다.
3. **업스트림 관계** — 이 저장소는 포크다. 본가는 별도 조직이 사실상 1인 체제로 유지 중이다. 기여자로 합류할지, 독자 노선으로 갈지, 협업을 제안할지를 수익화 이전에 정해두는 편이 안전하다.

---

## 결론

다섯 모델 중 넷은 나중에 해도 된다. 지금 할 일은 하나다 —
**한국 서비스 스킬 한 개를 만들어 2주 안에 실제 수요가 있는지 확인하는 것.**
